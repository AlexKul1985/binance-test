import { QuantityTuple } from "../model/types";

export const QuantityList: QuantityTuple = [100, 500, 1000]

export enum RequestStatus {
  Pending = 'Pending',
  Init = 'Init',
  Success = 'Success',
}
